package com.billfelipe.datasy.model;

import org.junit.Assert;
import org.junit.Test;

public class ObservationTest {

//	static Observation observation = new Observation((Input) InputTest.input, (Output) OutputTest.output);

//	@Test
//	public void test() {
//		Assert.assertNotNull(observation);
//		Assert.assertNotNull(observation.getInput());
//		Assert.assertNotNull(observation.getOutput());
//		Assert.assertNotNull(observation.getInput().getValues());
//		Assert.assertNotNull(observation.getOutput().getValues());
//		Assert.assertEquals(InputTest.values.length, observation.getInput().getValues().length);
//		Assert.assertEquals(OutputTest.values.length, observation.getOutput().getValues().length);
//		for (int i = 0; i < InputTest.values.length; i++) {
//			Assert.assertEquals(InputTest.values[i], observation.getInput().getValues()[i], 0.001);
//		}
//		for (int i = 0; i < OutputTest.values.length; i++) {
//			Assert.assertEquals(OutputTest.values[i], observation.getOutput().getValues()[i], 0.001);
//		}
//	}

}
